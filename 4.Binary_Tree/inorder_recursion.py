class Node:
	def __init__(self,data):
		self.data=data
		self.left=None
		self.right=None
def inorder(tremp):
	if(not temp):
		return
	inorder(temp.left)
	print(temp.data)
	inorder(temp.right)
